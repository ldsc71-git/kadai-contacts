<div class="text-center">
  <h1>連絡先詳細</h1>
</div>

<table class="table table-bordered">
  <tr>
    <th style="width: 30%;">名前</th>
    <td style="width: 70%;"><%= @contact.name %></td>
  </tr>
  <tr>
    <th>電話番号</th>
    <td><%= link_to @contact.phone, "tel:#{@contact.phone}" %></td>
  </tr>
  <tr>
    <th>メールアドレス</th>
    <td><%= mail_to @contact.email, @contact.email %></td>
  </tr>
  <tr>
    <th>メモ</th>
    <td><%= @contact.memo.gsub(/\r\n|\r|\n/, "<br>").html_safe %></td>
  </tr>
</table>

<div class="text-center">
  <%= link_to '編集', edit_contact_path(@contact), class: 'btn btn-light' %>
  <%= link_to '削除', @contact, method: :delete, data: { confirm: '本当に削除してよろしいですか？' }, class: 'btn btn-danger' %>
</div>